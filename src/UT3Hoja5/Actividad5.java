package UT3Hoja5;

public class Actividad5 {
    public boolean isEven;
    //Private boolean isEven; El error esta en el Private ya que deberia estar en minusculas
    //private boolean isOdd El error esta en el ; que le falta
    //public boolean is Odd; El error esta en que el is y el Odd estan separados y cuenta como si fueran parametros distintos
    //string S; Le falta que en el inicio tuviera un public o un private
    //public String boolean; Podria ser correcto si la b del boolean estuviera en mayuscula, pese a que esta el string delante el boolean cuenta como boolean en vez de un string con ese nombre
    //private boolean even = 0; El error esta en el tipo de declaracion que es, para que fuera correcto deberia ser un int
    //private String s = helloWorld; El error de sintaxis se encuentra en el resultado del string "s", le falta añadir las comillas



}
