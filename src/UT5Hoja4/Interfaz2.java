package UT5Hoja4;

public interface Interfaz2 {
    public final int x = 32;
    //public double y; El error que hay aqui es que el valor "y" no tiene valor
    /* Los errores que tenemos aqui son que hay 2 funciones con el mismo nombre
    public default double addup();

    public boolean addup();
*/
}
